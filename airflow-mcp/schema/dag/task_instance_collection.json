{
  "type": "object",
  "description": "Paginated collection of task instances",
  "properties": {
    "task_instances": {
      "type": "array",
      "items": { "$ref": "./task_instance.json" },
      "description": "Array of task instance objects"
    },
    "total_entries": {
      "type": "integer",
      "description": "Total number of matching task instances",
      "minimum": 0
    }
  },
  "required": ["task_instances", "total_entries"]
}
